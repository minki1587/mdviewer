# 변경 내역

릴리스 노트는 GitHub 릴리스 페이지에도 그대로 올라갑니다.

## 다음 버전 (아직 릴리스하지 않음)

- **Electron 에서 Tauri 2 로 옮겼습니다.** Chromium 을 통째로 들고 다니는 대신
  Windows 에 이미 있는 WebView2 를 씁니다.
  - 설치 파일 **107MB → 2.5MB**, 설치 후 차지하는 자리 **377MB → 약 5.5MB**
  - 실행 중 메모리 **약 123MB → 약 35MB**
  - 화면(`renderer/`) 은 그대로입니다. 창·파일·메뉴처럼 시스템에 닿는 일만
    Rust 로 옮겼고, 화면은 예전과 같은 `window.api` 를 봅니다.
- 자동 업데이트가 Tauri 업데이터로 바뀌었습니다. 릴리스에 올라가는 파일이
  `latest.yml` 에서 `latest.json` + 서명(`.sig`) 으로 바뀌고, 내려받은 설치
  파일의 서명이 맞을 때만 설치합니다.
- 문서에 딸린 로컬 이미지는 Tauri 의 asset 프로토콜로 들어옵니다.
- 빌드에 Rust 와 MSVC 빌드 도구가 필요해졌습니다. README 1장을 보세요.
- 없어진 것: 최근 문서 목록(Windows 점프 목록). Tauri 에 대응하는 기능이
  아직 없습니다.

## 1.3.1

- **업데이트 확인이 항상 "저장소가 지정되지 않았습니다"로 끝나던 문제 수정.**
  설치본의 `package.json` 에는 `build` 필드가 없는데(electron-builder 가
  패키징할 때 지웁니다) 그 값으로 발행 설정을 판단하고 있었습니다. 이제
  `app-update.yml` 을 읽습니다. 1.3.0 에서는 자동 업데이트가 아예 시작되지
  않았습니다.

## 1.3.0

- GitHub 릴리스를 통한 자동 업데이트 추가. 앱이 새 버전을 감지하면
  오른쪽 아래에 알림이 뜨고, 내려받아 다시 시작하면 설치됩니다.
- 도움말에 **업데이트 확인** 메뉴 추가.

## 1.2.0

- 도움말에 마크다운 문법 목록 추가 (`F1`). 문법 옆에 실제 렌더링 결과를
  함께 보여주고, 예시를 커서 자리에 바로 넣을 수 있습니다.
- 단축키 목록 탭 추가.

## 1.1.0

- 여러 문서를 탭으로 열기. 탭마다 본문, 커서, 되돌리기 기록, 스크롤 위치가
  따로 유지됩니다.
- 탭 순서 바꾸기, 가운데 버튼으로 닫기, `Alt+1`~`Alt+9` 로 이동.
- 열린 탭의 파일을 모두 감시.

## 1.0.1

- 렌더러가 시작조차 못 하던 문제 수정. `contextBridge` 가 만든 전역과
  최상위 `const api` 선언이 충돌해 스크립트 전체가 실행되지 않았습니다.
- 편집기에 파일을 떨어뜨리면 내용이 본문에 붙던 문제 수정.
- 상단 바에 새 문서 버튼 추가.

## 1.0.0

- 첫 릴리스. 마크다운 뷰어 겸 편집기, 나란히 보기, 실시간 미리보기,
  제목 기준 스크롤 동기화.
