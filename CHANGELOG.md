# 변경 내역

릴리스 노트는 GitHub 릴리스 페이지에도 그대로 올라갑니다.

## 2.1.0 (미출시)

- **읽기 화면의 좌우 여백을 조절할 수 있습니다.** 상단의 본문 너비 단추에서
  슬라이더를 움직이거나 `넓게`·`기본`·`좁게`를 고를 수 있습니다. 선택한 너비는
  저장되어 다음 실행에도 유지됩니다.
- **코드 블록에 복사 단추.** 코드 위에 마우스를 올리면 오른쪽 위에 단추가
  나타나고, 누르면 그 블록이 통째로 클립보드에 들어갑니다. 드래그해서 긁을
  필요가 없습니다.
- **할 일 목록을 읽기 화면에서 눌러 켭니다.** 예전에는 미리보기의 체크박스가
  잠겨 있어 편집 화면으로 가야 했습니다. 이제 누르면 원문의 그 줄이 `[ ]` 와
  `[x]` 사이로 바뀝니다. 한 글자만 고치므로 되돌리기도 그 한 글자만 되돌립니다.
  코드 블록 안에 적힌 `- [ ]` 는 세지 않고, 인용문 안의 할 일 목록은 셉니다.
- **파일 메뉴에 '최근 문서'.** 최근에 연 문서 10개가 쌓입니다. 저장으로 이름이
  생긴 문서도 함께 들어갑니다.
- **읽기 화면에도 상태 표시줄이 나옵니다.** 단어 수와 글자 수, 그리고 읽는 데
  걸릴 대략의 시간을 보여 줍니다. 읽는 중에야말로 분량이 궁금하기 때문입니다.
- **열어 둔 문서를 기억합니다.** 앱을 닫았다 열면 지난번 탭이 그대로 돌아오고,
  보고 있던 탭이 다시 활성 탭이 됩니다. `.md` 파일을 더블클릭해 실행한 경우에는
  그 문서가 활성 탭이 됩니다. 그 사이 파일이 지워졌거나 옮겨졌으면 몇 개를 찾지
  못했는지 알려 줍니다. 저장한 적 없는 새 문서는 되살릴 경로가 없어 빠집니다.
- **창 크기와 위치를 기억합니다.** 매번 같은 크기로 뜨던 것이 마지막으로 쓰던
  모습으로 열립니다. 최대화 상태도 함께 기억합니다.
- **읽기 화면에서 `Ctrl+F` 로 본문을 찾습니다.** 예전에는 나란히 보기로 바뀌면서
  편집기의 찾기가 열려, 읽기만 하려던 사람 앞에 편집 화면이 튀어나왔습니다.
  이제 읽는 화면 그대로 두고 결과에 색을 입히며, `Enter` / `Shift+Enter` 로
  다음·이전으로 옮겨 갑니다. 태그에 걸쳐 있는 낱말(**굵게** 가 중간에 낀 경우
  등)도 찾습니다. 편집 중일 때는 예전처럼 편집기의 찾기·바꾸기가 열립니다.
- **단축키가 동작하지 않던 문제 수정.** Ctrl+N, Ctrl+O, Ctrl+P, Ctrl+W, Ctrl+R,
  Ctrl+E, Ctrl+D, Ctrl+\\, Ctrl+= / Ctrl+-, Ctrl+0, Ctrl+Tab, F1, F11, F12 가
  모두 메뉴에만 적혀 있고 실제로는 눌러도 아무 일이 없었습니다. 메뉴를 마우스로
  누르면 동작하는데 같은 항목의 단축키만 죽어 있던 것이 증상이었습니다.
  원인은 WebView2 가 창 안의 키 입력을 먼저 가져가, Tauri 가 등록한 네이티브
  메뉴의 액셀러레이터 테이블까지 키가 내려가지 않는 것이었습니다. 이제 키는
  키가 실제로 도착하는 웹뷰 안에서 받습니다. 메뉴의 단축키 표시는 그대로입니다.
- 편집 중에 쓰던 키(Ctrl+B / I / K / S, Ctrl+1~3 등)는 예전처럼 편집기가 먼저
  가져갑니다. 읽기 모드에서만 위의 표가 맡으므로 저장이 두 번 되는 일은 없습니다.
- F11(전체 화면)을 단축키 목록에 맞춰 실제로 붙였습니다. 예전에는 단축키가
  적혀 있지 않은 기본 제공 메뉴 항목이었는데, 렌더러가 부를 수 있도록 직접
  창을 전환하는 항목으로 바꿨습니다.
- **회귀 테스트와 버전 검사를 추가했습니다.** 세션 복원, 한글 IME 단축키,
  할 일 체크박스 줄 매칭, 읽기 화면 검색을 자동으로 확인합니다. 앱 버전이
  `package.json`, `package-lock.json`, `Cargo.toml`, `tauri.conf.json` 중 한 곳이라도
  어긋나거나 릴리스 태그와 다르면 빌드 전에 멈춥니다.
- Rust 의존성을 `Cargo.lock`에 고정해 같은 소스가 시점에 따라 다른 라이브러리로
  빌드되는 일을 막았습니다. 이미 역할을 마친 Tauri 용량 측정 프로토타입과 전용
  워크플로도 제거했습니다.

## 2.0.2

- **PDF로 내보내기 추가** (`Ctrl+P`, 파일 메뉴, 상단 바 단추). 인쇄 대화상자에서
  "PDF로 저장"을 고르면 지금 탭의 읽기 화면이 그대로 PDF 가 됩니다. 종이에는
  상단 바·탭·목차·편집 창이 빠지고, 어두운 화면으로 보고 있어도 흰 바탕에 검은
  글씨로 찍힙니다. 긴 코드 줄과 넓은 표는 줄을 접어 페이지 안에 담고, 접어 둔
  `<details>` 와 아직 안 불러온 그림도 인쇄 직전에 펼쳐 함께 내보냅니다.
- **업데이트 확인이 "Could not fetch a valid release JSON" 로 끝나던 문제 수정.**
  Tauri 2 는 `bundle.createUpdaterArtifacts` 가 꺼져 있으면(기본값) 서명된
  업데이터 산출물을 만들지 않습니다. 그래서 릴리스에 설치 파일만 올라가고
  `latest.json` 이 빠져, 앱이 보는 주소가 404 였습니다. 이 값을 켜서 다음
  릴리스부터 `latest.json` 이 함께 올라갑니다. 2.0.1 에 설치된 앱도 그
  릴리스가 올라오는 순간부터 업데이트를 찾습니다.
- 위 상황에서 영어 원문이 그대로 뜨던 것을 한국어 안내로 바꿨습니다.
- **업데이터 서명 키를 새로 만들었습니다.** 기존 키는 암호가 맞지 않아 서명할 수
  없었고, 손에 있던 키 파일은 앱에 박힌 공개키와 짝이 아닌 다른 키였습니다.
  공개키가 `5F248B86F96924F5` 에서 `C4D3913FD171E8BF` 로 바뀝니다.
  **2.0.1 이 설치된 컴퓨터는 다음 판을 한 번 손으로 설치해야 합니다** — 옛 공개키로는
  새 서명을 검증할 수 없기 때문입니다. 그 뒤부터는 다시 자동으로 갱신됩니다.
- 빌드 전에 서명 키·암호·공개키가 맞는지 확인하는 검사를 넣었습니다.

## 2.0.1

- **창이 뜨지 않던 문제 수정.** 2.0.0 은 창을 숨긴 채 만들고 다시 보이게
  하는 코드가 없어, 프로세스는 떠도 창이 영원히 나타나지 않았습니다.
  이제 렌더러가 준비되면 창을 띄우고, 프런트엔드가 실패하더라도 3초 뒤
  무조건 띄웁니다.
- `npm run dev` (Electron 판) 가 깨져 있던 문제 수정. api 계층이 어느
  런타임에서 열렸는지 가려, Electron 이면 preload 가 만든 `window.api`
  를 건드리지 않습니다.
- main 푸시에 Electron 판을 발행하던 릴리스 워크플로를 Tauri 빌드 검증으로
  교체했습니다.

## 2.0.0

- **Electron 에서 Tauri 2 로 이식.** 설치 파일이 112MB 에서 2.5MB 로
  줄었습니다. 화면과 편집기는 그대로이고, `preload.js` 가 하던 일은
  `renderer/api-entry.js` 가 대신합니다.
- 자동 업데이트가 GitHub 릴리스의 `latest.json` 을 보도록 바뀌었습니다.

## 1.3.1

- **업데이트 확인이 항상 "저장소가 지정되지 않았습니다"로 끝나던 문제 수정.**
  설치본의 `package.json` 에는 `build` 필드가 없는데(electron-builder 가
  패키징할 때 지웁니다) 그 값으로 발행 설정을 판단하고 있었습니다. 이제
  `app-update.yml` 을 읽습니다. 1.3.0 에서는 자동 업데이트가 아예 시작되지
  않았습니다.

## 1.3.0

- GitHub 릴리스를 통한 자동 업데이트 추가. 앱이 새 버전을 감지하면
  오른쪽 아래에 알림이 뜨고, 내려받아 다시 시작하면 설치됩니다.
- 도움말에 **업데이트 확인** 메뉴 추가.

## 1.2.0

- 도움말에 마크다운 문법 목록 추가 (`F1`). 문법 옆에 실제 렌더링 결과를
  함께 보여주고, 예시를 커서 자리에 바로 넣을 수 있습니다.
- 단축키 목록 탭 추가.

## 1.1.0

- 여러 문서를 탭으로 열기. 탭마다 본문, 커서, 되돌리기 기록, 스크롤 위치가
  따로 유지됩니다.
- 탭 순서 바꾸기, 가운데 버튼으로 닫기, `Alt+1`~`Alt+9` 로 이동.
- 열린 탭의 파일을 모두 감시.

## 1.0.1

- 렌더러가 시작조차 못 하던 문제 수정. `contextBridge` 가 만든 전역과
  최상위 `const api` 선언이 충돌해 스크립트 전체가 실행되지 않았습니다.
- 편집기에 파일을 떨어뜨리면 내용이 본문에 붙던 문제 수정.
- 상단 바에 새 문서 버튼 추가.

## 1.0.0

- 첫 릴리스. 마크다운 뷰어 겸 편집기, 나란히 보기, 실시간 미리보기,
  제목 기준 스크롤 동기화.
